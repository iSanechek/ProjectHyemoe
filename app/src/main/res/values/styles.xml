<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/my_primary_color</item>
        <item name="colorPrimaryDark">@color/my_primary_dark_color</item>
        <item name="colorAccent">@color/my_accent_color</item>
        <item name="android:windowBackground">@color/colorPrimary_light</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="WallButtonStyle" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/my_accent_color</item>
    </style>

    <style name="WallToolbarTheme">
        <item name="theme">@style/ThemeOverlay.AppCompat.Dark</item>
        <item name="android:background">?colorPrimary</item>
        <item name="android:elevation">4dp</item>
    </style>

    <!--Set Wallpaper style-->
    <style name="InstallBtn" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/my_accent_color</item>
    </style>

</resources>
